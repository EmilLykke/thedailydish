import { StyleSheet } from "react-native";

const styles = (num?:number) => StyleSheet.create({
    recipeListContainer:{
        width: "100%",
        flexDirection: "row",
        flexWrap: "wrap",
    },
})

export default styles;